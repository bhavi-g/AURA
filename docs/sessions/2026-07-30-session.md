# Session — 2026-07-30

**Phase:** `docs/phases/phase-1.md` — Verified fixes (closed loop)
**Model:** Sonnet for implementation/task-review subagents; Opus for the final whole-branch review (per CLAUDE.md's model-discipline rule — architecture/security-level review warranted the more capable model)

## Start-of-session (read first)
- [x] Read the latest `docs/sessions/` log (2026-07-24)
- [x] Read the ADR index (`docs/adr/README.md`)
- [x] Confirmed working from `docs/phases/phase-1.md`

## WHAT I BUILT
- Design spec (`docs/superpowers/specs/2026-07-25-verified-fix-loop-design.md`) and implementation plan (`docs/superpowers/plans/2026-07-25-verified-fix-loop.md`) for Phase 1.
- New `src/aura/core/fix.py`: `VerifyResult`, `_finding_key`, `_compile_with_solc`, `_run_git_apply`, `generate_fix_diff`, `verify_fix` (single-attempt: apply diff in isolated temp workspace → control-run the analyzer against the pristine copy → compile → re-analyze patched copy → compare), `verify_fix_loop` (retries up to 3, feeding failures back as `error_context`).
- Rewired `aura fix` (`src/aura/cli.py`) to call `verify_fix_loop`; added `--write` (only writes on `VERIFIED`), `--json`, `--max-retries`.
- Built via subagent-driven development: 8 planned tasks, each with an independent implementer + task review, plus a final whole-branch review and one fix wave.

## WORKS (verified — how?)
- 63/63 tests passing locally and in CI (both `test` matrix runs + `bench`), confirmed independently after every task/fix, not just from subagent self-reports.
- Manually verified `VERIFIED` end-to-end against `contracts/Reentrancy.sol` with real `solc 0.8.30`/`slither 0.11.3`.
- Final whole-branch review (dispatched on Opus) found and — after explicit approval — fixed two **Critical** security issues, both confirmed closed via independent exploit re-reproduction (not just code reading):
  1. Verification could fail open: a crashed/timed-out analyzer looked identical to "no findings," so a broken analyzer run could yield `VERIFIED` for an untouched vulnerability. Fixed with a control run against the pristine file before trusting the patched result.
  2. `git apply --unsafe-paths` allowed a crafted diff to write outside the temp verification workspace, independent of `--write`. Fixed by canonicalizing diff headers to the target's basename and dropping the flag.
  3. (Important, fixed alongside) relative target paths — the format shown in the README — silently failed to apply; only absolute paths (what every test happened to use) worked.
- Merged as PR #26 (`7d7fec6`), squash, into `main`.

## LEFT (what's unfinished)
- Phases 2–5 not started (Claude LLM backend, PyPI publish, GitHub Action, deployed backend/paste-mode fix).
- Backlog surfaced by the final review, deliberately out of Phase 1's scope:
  - `explain.py::summarize_findings` crashes (`IndexError`) on findings with an empty `description`.
  - `_analyzer_available()` doesn't account for the `pipx` fallback path.
  - No subprocess timeout on `git apply`/`git init`.
  - `aura fix` crashes uncaught on a directory target instead of a single file.
  - `REGRESSED` verdict's `new_findings` isn't surfaced in CLI text or `--json` output.
  - `fix_cmd` always exits 0, even on `FAILED`/`REGRESSED`.
  - Local `.venv/bin/pre-commit`'s shebang still points at the pre-move project path (`~/aura`) — only the git hook wrapper was fixed earlier this session, not this script.

## BLOCKERS
- None currently.

## Decisions → new ADRs?
- No new ADR this session — Phase 1's design decision was already captured in `docs/adr/0004-verified-fix-loop.md` prior to implementation. Its `Status:` should be flipped from `proposed` to `accepted` now that it's merged (left as a follow-up; not done in this session).

## End-of-session
- [x] Session log written (this file)
- [ ] Real decisions logged as ADRs — n/a this session (see above); ADR 0004 status flip still pending
- [ ] Committed + pushed
- [ ] Tomorrow's stub — next session should pick a Phase 2–5 priority, or clear the small backlog list above first

---

# Session — 2026-07-30 (second session: Phase 2 kickoff)

**Phase:** `docs/phases/phase-2.md` — Claude LLM backend + provider abstraction
**Model:** Sonnet for implementer/task-review subagents; Opus for the final whole-branch review (per CLAUDE.md's model-discipline rule)

## Start-of-session (read first)
- [x] Read the latest `docs/sessions/` log (this same file, Phase 1 entry above)
- [x] Read the ADR index (`docs/adr/README.md`)
- [x] Confirmed working from `docs/phases/phase-2.md`
- [x] Read `docs/NEVER_DO.md`

## WHAT I BUILT
- Design spec (`docs/superpowers/specs/2026-07-30-claude-llm-backend-design.md`) and implementation plan (`docs/superpowers/plans/2026-07-30-claude-llm-backend.md`) for Phase 2's LLM-backend slice.
- `src/aura/core/llm.py` refactored from a single OpenAI-only `LLM` class into a thin facade (`LLM`) over three backend classes: `_StubBackend`, `_OpenAIBackend` (behavior-preserving), and a new `_AnthropicBackend` (default model `claude-sonnet-5`) with two deliberate deviations required for correctness on the real API: never forwards `temperature`/`top_p`/`top_k`, and unconditionally disables thinking (`thinking={"type": "disabled"}`) to avoid adaptive thinking silently consuming the response's `max_tokens` budget.
- Provider resolution (`LLM._resolve_backend`): `LLMConfig.provider` (explicit) > `AURA_LLM_PROVIDER` env var > auto-detect (Anthropic first, then OpenAI, then stub); an explicitly-requested-but-unavailable provider degrades straight to stub rather than falling through to the other.
- `anthropic` added as a hard dependency (mirroring `openai`'s existing precedent).
- New `tests/conftest.py` with an autouse fixture clearing `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`AURA_LLM_PROVIDER` for every test by default — added during the final review after Task 3's Anthropic-first auto-detect made the previously-safe `tests/test_llm_stub.py` (frozen by the plan) environment-fragile.
- `.env.example` updated to document `ANTHROPIC_API_KEY` and the `AURA_LLM_PROVIDER` precedence/auto-detect rules.
- Built via subagent-driven development: 3 planned tasks, each with an independent implementer + task review; one task-level fix round (Task 3: two resolution tests needed env-var isolation once Anthropic entered auto-detection); a final whole-branch review (dispatched on Opus) that verified all four load-bearing correctness claims directly against the code (not the task reports) and found two additional Important issues, both fixed and re-reviewed clean.

## WORKS (verified — how?)
- 77/77 tests passing locally, confirmed independently after every task, the task-level fix round, and the final-review fix wave — not just from subagent self-reports.
- Final whole-branch review (Opus) cross-checked the two Anthropic-specific deviations against Anthropic's live API documentation rather than trusting the spec, and empirically confirmed the `anthropic` version-floor bug (0.40.0 predates the `thinking` SDK parameter Task 2's code depends on — anything below 0.47.0 would `TypeError` on every real call) by testing against an installed SDK.
- Provider-resolution precedence matrix (9 tests) covers explicit config, env var, auto-detect ordering, explicit-but-unavailable → stub (not fallthrough), and unrecognized-value → auto-detect (not stub) — all traced against the actual `if`/`elif` logic by both the task reviewer and the final reviewer independently.
- Pushed and opened as PR #28 against `main`; branch protection requires PR review + CI (matching PRs #23–27's precedent), so merge is pending review at time of writing.

## LEFT (what's unfinished)
- PR #28 not yet merged — awaiting review/CI on GitHub.
- Deferred Minor findings from the task and final reviews (none block merge; the SDD ledger they were tracked in has been deleted per the subagent-driven-development skill's cleanup step, so summarizing here for the record):
  - No `typing.Protocol` for the informal `acomplete` backend contract shared by `_StubBackend`/`_OpenAIBackend`/`_AnthropicBackend`.
  - `SYSTEM_GUARD` in `_AnthropicBackend` names `<thinking>` tags explicitly; Anthropic's own guidance recommends generic wording ("Do not include internal or system XML tags") as measurably more effective — spec-faithful as shipped, worth an A/B follow-up.
  - `.env.example` doesn't document the "explicit-but-unavailable provider degrades to stub rather than falling back to the other provider" rule.
  - Missing test: `LLMConfig.provider` overriding a conflicting `AURA_LLM_PROVIDER` when both keys are present (code is correct, just untested).
  - Phase 2's "improve explain/fix prompts now that we control the provider" line was deliberately deferred out of this change's scope (see the design spec's Out of Scope section) — worth revisiting once Claude's output on the *current* prompts can be compared against the old OpenAI output.
- Phases 3–5 not started (PyPI publish, GitHub Action, deployed backend/paste-mode fix).
- Carried over from the 2026-07-24 Phase 1 session's backlog (untouched this session, still open): `explain.py::summarize_findings` IndexError on empty description; `_analyzer_available()` pipx fallback gap; no subprocess timeout on `git apply`/`git init`; `aura fix` crashes on a directory target; `REGRESSED` verdict's `new_findings` not surfaced in CLI/JSON; `fix_cmd` always exits 0; local `.venv/bin/pre-commit` shebang.

## BLOCKERS
- None currently. PR #28's merge is pending human/CI action, not a blocker on further work.

## Decisions → new ADRs?
- No new ADR needed — ADR 0003 already anticipated exactly this change ("Next: generalize to a provider interface and add an Anthropic/Claude backend, defaulting to the latest cost-effective Claude model"). Updated ADR 0003's body to reflect the now-implemented state instead of filing a new one.

## End-of-session
- [x] Session log written (this file, second entry)
- [x] ADR 0003 updated to reflect implementation (not a new ADR)
- [ ] Committed + pushed — pending (this log + ADR update go in the next commit, onto the PR #28 branch)
- [ ] Tomorrow's stub — next session should pick up PR #28's review feedback if any, then move to Phase 3 (PyPI publish) or clear the remaining Minor/backlog items above
